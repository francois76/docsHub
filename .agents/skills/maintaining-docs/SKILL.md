---
name: maintaining-docs
description: Information about how to read and write documentation on start and end of work
---

# instructions

The project documentation lives in the `docs/` folder at the root of the repository. Reading it before beginning a job is REQUIRED.

Every code change MUST be followed by updating the affected documentation in `docs/` before the task is considered done.
