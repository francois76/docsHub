# NextAuth.js — Required
NEXTAUTH_SECRET=change-me-to-a-random-32-char-string
NEXTAUTH_URL=http://localhost:3000

# GitHub OAuth (optional — enables OAuth-based reviews on GitHub repos)
# Create an OAuth App at: https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# GitLab OAuth (optional — enables OAuth-based reviews on GitLab repos)
# Create an OAuth App at: https://gitlab.com/-/user_settings/applications
GITLAB_CLIENT_ID=
GITLAB_CLIENT_SECRET=

# Per-repo tokens (referenced in .docshub.yml via token: $MY_GITHUB_TOKEN)
# Example:
#   repos:
#     - name: my-repo
#       type: github
#       url: https://github.com/org/my-repo.git
#       token: $MY_GITHUB_TOKEN   # or ${MY_GITHUB_TOKEN}
MY_GITHUB_TOKEN=
